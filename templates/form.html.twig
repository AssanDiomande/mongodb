{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}
    <h1>{{ title }}</h1><br>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">Valider</button> <a class="btn btn-primary" href="{{ path('app_user_read') }}">Retour</a>
    {{ form_end(form) }}
{% endblock %}